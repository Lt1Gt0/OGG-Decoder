#pragma once
#ifndef _COMMON_H
#define _COMMON_H

#include <stdint.h>

// Vorbis spec refers to bytes as octets alot so this is to make it more clear
typedef uint8_t byte;
typedef byte octet;

#endif // _COMMON_H
